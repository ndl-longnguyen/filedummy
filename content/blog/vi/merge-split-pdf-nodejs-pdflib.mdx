Thao tác trang PDF — kết hợp nhiều báo cáo thành một tệp hoặc trích xuất các trang cụ thể — có thể được thực hiện hoàn toàn bằng JavaScript thuần túy mà không cần các phụ thuộc hệ thống bên ngoài như Poppler hay Ghostscript, nhờ thư viện `pdf-lib`.

## Cài Đặt pdf-lib

```bash
npm install pdf-lib
```

<BlogDownloadCTA
  fileType="pdf"
  fileSlug="10mb"
  message="Tải file PDF mẫu 10MB nhiều trang để kiểm thử các hàm tách và ghép với pdf-lib."
  buttonLabel="Tải File Mẫu 10MB PDF →"
/>

## Ghép Nhiều PDF

```typescript
import { PDFDocument } from 'pdf-lib';
import fs from 'fs';

export async function mergePdfFiles(pdfBuffers: Buffer[]): Promise<Uint8Array> {
  const mergedPdf = await PDFDocument.create();

  for (const buffer of pdfBuffers) {
    const donorPdf = await PDFDocument.load(buffer);
    const copiedPages = await mergedPdf.copyPages(donorPdf, donorPdf.getPageIndices());

    copiedPages.forEach((page) => mergedPdf.addPage(page));
  }

  return await mergedPdf.save();
}
```

## Tách PDF (Trích Xuất Phạm Vi Trang)

```typescript
import { PDFDocument } from 'pdf-lib';

export async function extractPdfPages(
  sourceBuffer: Buffer,
  pageIndices: number[] // số trang theo chỉ mục 0
): Promise<Uint8Array> {
  const sourcePdf = await PDFDocument.load(sourceBuffer);
  const newPdf = await PDFDocument.create();

  const copiedPages = await newPdf.copyPages(sourcePdf, pageIndices);
  copiedPages.forEach((page) => newPdf.addPage(page));

  return await newPdf.save();
}
```

## Mẹo Hiệu Năng Trong Bộ Nhớ
- **Giữ luồng byte trong bộ nhớ:** Tránh ghi các tệp trung gian ra đĩa trong quá trình ghép nhiều bước.
- **Bảo toàn metadata:** Sử dụng `mergedPdf.setTitle('Báo Cáo Tổng Hợp')` và `mergedPdf.setAuthor('My App')` để duy trì thuộc tính tài liệu gọn gàng.