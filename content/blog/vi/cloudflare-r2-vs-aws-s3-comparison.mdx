Amazon S3 từ lâu đã là giải pháp lưu trữ object storage đám mây thống lĩnh thị trường, nhưng phí truyền dữ liệu egress (băng thông) vẫn là điểm đau chi phí đáng kể cho các nền tảng tải xuống có lưu lượng cao. Cloudflare R2 gia nhập thị trường với khả năng tương thích S3 API hoàn toàn và phí egress bằng không. Dưới đây là bảng phân tích kiến trúc và tài chính.

## Sự Khác Biệt Kiến Trúc Cốt Lõi

| Tính năng | AWS S3 Standard | Cloudflare R2 |
|-----------|-----------------|---------------|
| Tương thích S3 API | Gốc | Tương thích 99% (SigV4) |
| Phí Egress | $0.09 / GB (Mỹ/EU) | **$0.00 / GB (Miễn phí)** |
| Chi phí lưu trữ | $0.023 / GB / tháng | $0.015 / GB / tháng |
| Tích hợp CDN toàn cầu | Cần CloudFront (tính phí) | Tích hợp Cloudflare CDN trực tiếp |
| Thời gian lưu trữ tối thiểu | Không có với Standard (90 ngày với Glacier) | Không có |

<BlogDownloadCTA
  fileType="pdf"
  fileSlug="100mb"
  message="Kiểm thử tải xuống độ trễ thấp được phục vụ trực tiếp từ Cloudflare R2 edge object storage."
  buttonLabel="Tải File Mẫu 100MB PDF →"
/>

## Migration Code: Kết Nối Đến R2 Với AWS SDK v3

Chuyển đổi từ S3 sang R2 chỉ cần thay đổi endpoint của client:

```typescript
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';

const r2Client = new S3Client({
  region: 'auto',
  endpoint: `https://${process.env.CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com`,
  credentials: {
    accessKeyId: process.env.R2_ACCESS_KEY_ID!,
    secretAccessKey: process.env.R2_SECRET_ACCESS_KEY!,
  },
});

export async function uploadToR2(bucket: string, key: string, data: Buffer) {
  return await r2Client.send(
    new PutObjectCommand({
      Bucket: bucket,
      Key: key,
      Body: data,
    })
  );
}
```

## Khi Nào Nên Chọn Cái Nào?
- **Chọn Cloudflare R2:** Cho phân phối tệp công khai, phát hành phần mềm, game patcher, và tài sản video nơi chi phí egress tải xuống chiếm phần lớn hóa đơn AWS của bạn.
- **Chọn AWS S3:** Nếu ứng dụng của bạn phụ thuộc sâu vào các dịch vụ AWS như Athena, SageMaker, hoặc gắn tag tài nguyên IAM chi tiết.