Cho phép người dùng tải xuống tài sản riêng tư trực tiếp từ Amazon S3 hoặc Cloudflare R2 mà không cần định tuyến luồng dữ liệu khổng lồ qua máy chủ ứng dụng của bạn sẽ giảm đáng kể tải cho máy chủ và chi phí băng thông. Presigned URL cung cấp các liên kết tải xuống an toàn, có thời hạn, được ký bởi thông tin xác thực đám mây của bạn.

## Kiến Trúc: Tại Sao Dùng Presigned URL Cho Download Trực Tiếp?

```
[Trình duyệt] -- 1. GET /api/download-link?file=report.pdf --> [Node.js API Server]
                                                                 | Tạo Presigned URL
[Trình duyệt] <-- 2. Trả về URL có chữ ký (hết hạn sau 15 phút) (Không tốn băng thông!)

[Trình duyệt] -- 3. GET https://bucket.r2.cloudflarestorage.com/report.pdf?X-Amz-Signature=... --> [Cloudflare R2]
[Trình duyệt] <-- 4. Tải xuống trực tiếp tốc độ cao -----------------------------------------------+
```

<BlogDownloadCTA
  fileType="pdf"
  fileSlug="100mb"
  message="Tải file PDF mẫu 100MB để đo thời gian đến byte đầu tiên (TTFB) với presigned download URL."
  buttonLabel="Tải File Mẫu 100MB PDF →"
/>

## Tạo Presigned GET URL Trong Node.js (AWS SDK v3)

```typescript
import { S3Client, GetObjectCommand } from '@aws-sdk/client-s3';
import { getSignedUrl } from '@aws-sdk/s3-request-presigner';

const s3 = new S3Client({
  region: 'auto',
  endpoint: process.env.R2_ENDPOINT,
  credentials: {
    accessKeyId: process.env.AWS_ACCESS_KEY_ID!,
    secretAccessKey: process.env.AWS_SECRET_ACCESS_KEY!,
  },
});

export async function createDownloadUrl(
  bucketName: string,
  objectKey: string,
  fileName: string,
  expiresInSeconds = 900 // 15 phút
): Promise<string> {
  const command = new GetObjectCommand({
    Bucket: bucketName,
    Key: objectKey,
    // Buộc trình duyệt tải về với tên file đúng thay vì mở trong tab mới
    ResponseContentDisposition: `attachment; filename="${encodeURIComponent(fileName)}"`,
  });

  const presignedUrl = await getSignedUrl(s3, command, { expiresIn: expiresInSeconds });
  return presignedUrl;
}
```

## Các Biện Pháp Bảo Mật Tốt Nhất
- **Thời gian hết hạn ngắn:** Giới hạn thời gian hết hạn từ 5–15 phút. Sau khi trình duyệt bắt đầu luồng tải xuống, tính liên tục của kết nối vẫn được duy trì ngay cả khi URL hết hạn giữa chừng.
- **Kiểm tra phân quyền trước:** Luôn xác minh quyền phiên người dùng và giới hạn tốc độ trong API endpoint trước khi tạo và trả về URL đã ký.